# Parámetros de la función `auth` para el proyecto de Firebase `migo-dev-20b41` (ambiente `dev`).
#
# El Client ID NO es un secreto: viaja en cada petición del navegador y ya está en el `config.json`
# publicado. Lo que sí lo es —el client secret— vive en Secret Manager, nunca aquí.
#
# TIENE que ser el mismo `googleClientId` que declara este ambiente en
# `deploy/firebase/environments.json`. Si divergen, Google rechaza el canje con `invalid_client` y
# el mensaje no dice por qué.
GOOGLE_OAUTH_CLIENT_ID=406941726541-6e6q7qlimm6lp8ptrtgt9gnui9p12eie.apps.googleusercontent.com
